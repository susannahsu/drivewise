import { DecisionStub } from "../_DecisionStub";

export default function Page() {
  return (
    <DecisionStub
      tag="US-3"
      title="Which brand & model?"
      summary="Rank candidate models by your chosen objective — lowest total cost, best reliability, slowest depreciation, lowest emissions, or a weighted mix — starting from the 20-car best-value shortlist in docs/CAR_SHORTLIST.md."
    />
  );
}
