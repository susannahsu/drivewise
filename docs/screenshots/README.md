# Screenshots

Drop PNGs here and uncomment the image lines near the top of the root
`README.md` to show them on the repo landing page. Good ones to capture:

- `guide-dashboard.png` — the guided-flow recommendation + four-module board
- `powertrain.png` — the break-even chart with what-if sliders
- `own-vs-rideshare.png` — the own / lease / rideshare crossover chart
- `model-picker.png` — the ranked shortlist with car photos
